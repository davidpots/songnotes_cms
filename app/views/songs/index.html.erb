<div class="bucket" style="border:none;">
  <h1>Songnotes</h1>
  <p style='margin: 20px 0 10px;'>Hand-crafted guitar tabs, chords, and lyrics all made by a <a href="http://twitter.com/davidpots">single person</a> who cares about good design.</p>
  <p>Keep posted of song additions (or make a tab request!) by following Songnotes on <a href="http://www.twitter.com/playsongnotes">Twitter</a>.

  <hr /><br />

  <ul class="sorter_nav">
    <li class="key">sort songs by:</li>
    <li class="<% if @nav == 'created_at' %>active<% end %>"><%= link_to "date added", :sort => "created_at", :direction => "ASC" %></li>
    <li class="<% if @nav == 'title' %>active<% end %>"><%= link_to "title", :sort => "title", :direction => "ASC" %></li>
    <li class="<% if @nav == 'year' %>active<% end %>"><%= link_to "year", :sort => "year", :direction => "DESC" %></li>
    <li class="<% if @nav == 'artist' %>active<% end %>"><%= link_to "artist", :sort => "artist", :direction => "ASC" %></li>
  </ul>

  <% if @nav == 'artist' %>

    <% @songs_by_artist.sort.each do |artist, songs| %>
      <ul class="directory">
        <h2><%= artist %></h2>
        <% for song in songs %>
          <li>
            <%= link_to(song) do %>
              <strong><%= song.title %></strong>
              <%= song.artist %> &bull; <%= song.year %>
            <% end %>
          </li>
        <% end %>
      </ul>
    <% end %>

  <% elsif @nav == 'year' %>

    <% @songs_by_year.each do |year, songs| %>
      <ul class="directory">
        <h2><%= year %></h2>
        <% for song in songs %>
          <li>
            <%= link_to(song) do %>
              <strong><%= song.title %></strong>
              <%= song.artist %> &bull; <%= song.year %>
            <% end %>
          </li>
        <% end %>
      </ul>
    <% end %>

  <% elsif @nav == 'created_at' %>
      <ul class="directory">
    <% @songs_by_created_at.each do |created_at, songs| %>
        <% for song in songs %>
          <li>
            <%= link_to(song) do %>
              <strong><%= song.title %></strong>
              <%= song.artist %> &bull; <%= song.year %>
              <span class="alt"><%= time_ago_in_words(created_at) %> ago</span>
            <% end %>
          </li>
        <% end %>
    <% end %>
      </ul>
      
  <% elsif @nav == 'title' %>

    <ul class="directory">
      <% @songs.each do |song| %>
      <li>
        <%= link_to(song) do %>
          <strong><%= song.title %></strong>
          <%= song.artist %> &bull; <%= song.year %>
        <% end %>
      </li>
      <% end %>
    </ul>

  <% end %>
  
  <p style="text-align: center;"><strong>More coming soon.</strong></p>
  <p style="text-align: center;">Is there a specific song you'd like to see added?<br /><a href="http://twitter.com/davidpots" target="_blank">Tweet at me</a> and I'll make it happen.</p>
</div>